# quantconnect-qmt-stubs

Python type stubs and the Python.NET runtime namespace loader for the
strategy-facing `QuantConnect.Brokerages.Qmt` API.

Install from the Brokerage repository root:

```bash
python -m pip install ./python_stubs
```

The QMT Brokerage DLL is still required when the strategy runs in LEAN.
